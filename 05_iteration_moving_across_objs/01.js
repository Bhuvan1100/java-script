for (let index = 0; index <10; index++) {
    const element = index;
    console.log(element);   
}
// nothing much in this topic  only basics that you have studied in dsa topics and cp
// only loops and nestes loops and some other topics 
const arr = [1,2,3,4];
console.log(arr.length)


// just doing my coding so that i can be good at this and do codind at a better pace and work on documents at a greater pace
// this lecture is quiet easy and has to be just seen at twice speed and require and no practise


for (let index = 1; index <= 8; index++) {
    if (index == 5) {
        console.log(`Detected 5`);
        continue; // after if no other line is exexuted
    }
    console.log(`Value of i is ${index}`);
   
    
}